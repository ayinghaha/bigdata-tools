<workflow-app xmlns='uri:oozie:workflow:0.1' name='UserClickLog2Avro'>
	<start to="UserClickLog2Avro" />
	<action name="UserClickLog2Avro">
		<java>
			<job-tracker>${jobTracker}</job-tracker>
			<name-node>${nameNode}</name-node>
			<configuration>
			<property>
		        <name>mapred.job.queue.name</name>
		 	<value>${queue}</value>
			</property>
			</configuration>
			<main-class>com.iflytek.maple.main.UserClickLog2AvroMain</main-class>
			<arg>${input}</arg>
			<arg>${output}</arg>
			<capture-output />
		</java>
		<ok to="end" />
		<error to="fail" />
	</action>
	<kill name="fail">
		<message>Failed, error message[${wf:errorMessage(wf:lastErrorNode())}]
		</message>
	</kill>
	<end name="end" />
</workflow-app>
