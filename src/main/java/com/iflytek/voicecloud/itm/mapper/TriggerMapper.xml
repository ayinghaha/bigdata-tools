<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iflytek.voicecloud.itm.dao.TriggerDao">
    <resultMap id="TriggerResult" type="Trigger" >
        <id property="id" column="id" />
        <result property="itmID" column="itm_id" />
        <result property="containerID" column="container_id"/>
        <result property="name" column="name"/>
        <result property="type" column="type"/>
        <result property="registTime" column="regist_time"/>
        <result property="updateTime" column="update_time"/>
        <association property="variableFilters" column="id" select="com.iflytek.voicecloud.itm.dao.VariableFilterDao.getFilterByTriggerId"/>
    </resultMap>

    <resultMap id="TagTriggerLinkResult" type="TagTriggerLink">
        <id property="id" column="id"/>
    </resultMap>

    <insert id="addTrigger" parameterType="Trigger" keyProperty="id" useGeneratedKeys="true">
        insert into t_trigger values(null, #{itmID}, #{containerID}, #{name}, #{type}, #{delay}, #{registTime}, #{updateTime})
    </insert>

    <select id="getTriggerById" parameterType="Integer" resultMap="TriggerResult">
        select * from t_trigger where id = #{id}
    </select>

    <select id="getTriggerByIdList" resultMap="TriggerResult">
        select * from t_trigger where id in
        <foreach collection="list" item="triggerId" index="index" open="(" close=")" separator=",">
            #{triggerId}
        </foreach>
    </select>

    <select id="getTagTriggers" resultMap="TriggerResult" parameterType="Tag">
        select tr.* from t_tag tag
        left join t_tag_trigger_link link on tag.id = link.tag_id
        left join t_trigger tr on tr.id = link.trigger_id
        where tag.id = #{id}
    </select>

    <select id="getTriggers" parameterType="Trigger" resultMap="TriggerResult">
        select * from t_trigger
        <where>
            <if test="id != null and id != 0">
                id = #{id}
            </if>
            <if test="itmID != null">
                AND itm_id = #{itmID}
            </if>
            <if test="containerID != null">
                AND container_id = #{containerID}
            </if>
            <if test="type != null">
                AND type = #{type}
            </if>
            <if test="name != null">
                AND name = #{name}
            </if>
        </where>
    </select>

    <select id="getLinkByTriggerId" parameterType="Integer" resultMap="TagTriggerLinkResult">
        select * from t_tag_trigger_link where trigger_id = #{triggerId}
    </select>

    <delete id="deleteByTriggerId" parameterType="Integer" >
        delete from t_trigger where id = #{triggerId}
    </delete>

    <update id="updateTrigger" parameterType="Trigger">
        update t_trigger
        <trim prefix="set" suffixOverrides=",">
            <if test="name != null">name = #{name},</if>
            <if test="type != null">type = #{type},</if>
            <if test="type == 'timer'">delay = #{delay}</if>
        </trim>
        where id = #{id}
    </update>

</mapper>